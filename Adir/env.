# Secret Key for Flask
SECRET_KEY=your_secret_key

# GCP SQL Credentials
DB_USERNAME=your_db_username          # שם המשתמש של בסיס הנתונים ב-GCP
DB_PASSWORD=your_db_password          # סיסמת המשתמש של בסיס הנתונים
DB_NAME=your_db_name                  # שם בסיס הנתונים
INSTANCE_CONNECTION_NAME=your_project:your_region:your_instance  # שם ה-Instance ב-GCP

# GitHub API Credentials
GITHUB_TOKEN=your_github_token        # טוקן הגישה ל-GitHub
GITHUB_REPO_URL=your_github_repo      # כתובת ה-Repo של GitHub (בפורמט: username/repo_name)
WORKFLOW_ID=Get_support_token.yml     # שם ה-Workflow ב-GitHub